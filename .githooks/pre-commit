#!/bin/sh
set -e

echo "pre-commit: ruff check..."
make lint

echo "pre-commit: ruff format --check..."
make format-check

echo "pre-commit: gitleaks (staged secrets)..."
docker run --rm -v "$(pwd):/repo" -w /repo zricethezav/gitleaks:latest protect --staged --redact -v
